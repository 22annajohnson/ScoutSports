# Jira

This directory documents Scout Jira organization, conventions, and delivery workflow.

Jira should translate approved technical plans into small, sequential, independently reviewable tickets. Tickets should be scoped so an implementation agent can complete each one in a few hours when possible.

See `JIRA_WORKFLOW.md` for project keys, epic conventions, ticket naming, and the full delivery pipeline.
